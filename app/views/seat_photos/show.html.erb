<div class="se-pre-con"></div>
<h2> Your Uploaded Seat Photo </h2>
<br>
<a href="/venues/<%= @seat.venue_id %>/seats/" role="button" class="back-button btn btn-default">Back to Seating Chart</a>

<h3>Section: <%= @seat.seat_section %>
<br>Row: <%= @seat.seat_row %>
<br>Seat: <%= @seat.seat_number %>
<br>Caption: <%= @seat_photo.caption %></h3>
<h5>Uploaded Image: </h5>
<%= image_tag @seat_photo.image.medium.url %>
<br><br>
<% if current_user && current_user.admin || current_user && current_user.id == @seat_photo.user_id %> 
  <div>
    <a data-confirm="Are you sure you want to edit this photo? Photo file cannot be edited. To change photo, please delete the entry and reupload the image and caption information" href="/seats/<%= @seat.id %>/seat_photos/<%= @seat_photo.id %>/edit" rel="nofollow" role="button" class="btn btn-default btn-xs active">Edit Seat Photo Info </a>
    <a data-confirm="Are you sure you want to delete this photo file?" data-method="delete" href="/seats/<%= @seat.id %>/seat_photos/<%= @seat_photo.id %>" rel="nofollow" role="button" class="btn btn-default btn-xs active">Delete Seat Photo </a>
  </div>
<% end %>

<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.5.2/jquery.min.js"></script>
<script src="http://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.2/modernizr.js"></script>
